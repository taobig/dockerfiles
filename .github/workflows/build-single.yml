name: Build Single Image

on:
  workflow_dispatch:
    inputs:
      php_version:
        description: "PHP version to build"
        required: true
        type: choice
        options:
          - php82 # 必须和 Dockerfile 目录名的后缀一致
          - php83
          - php84
      push_to_registry:
        description: "Push to Docker Hub"
        required: false
        type: boolean
        default: false

env:
  DOCKERFILE_DIR: ubuntu_nginx_${{ github.event.inputs.php_version }}
  IMAGE_NAME: taobig/nginx-${{ github.event.inputs.php_version }}  # Docker Hub 镜像名称，如 taobig/nginx-php84

jobs:
  # 使用矩阵策略在不同架构的原生 runners 上并行构建
  build:
    strategy:
      matrix:
        # PHP 8.4 支持多架构，旧版本只构建 AMD64
        arch: ${{ fromJSON(inputs.php_version == 'php84' && '["amd64","arm64"]' || '["amd64"]') }}

    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-24.04' }}

    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        if: ${{ inputs.push_to_registry }}
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: ./${{ env.DOCKERFILE_DIR }}
          file: ./${{ env.DOCKERFILE_DIR }}/Dockerfile
          platforms: linux/${{ matrix.arch }}
          pull: true  # 强制拉取最新的 base image
          # push-by-digest 只能给仓库名，带 :tag 会报 can't push tagged ref by digest
          tags: ${{ inputs.push_to_registry && env.IMAGE_NAME || format('{0}:latest-{1}', env.IMAGE_NAME, matrix.arch) }}
          provenance: false  # 避免 OCI digest 与 registry digest 不一致，merge 时找不到镜像
          outputs: ${{ inputs.push_to_registry && format('type=image,name={0},push-by-digest=true,name-canonical=true,push=true', env.IMAGE_NAME) || 'type=docker' }}

      - name: Export digest
        if: ${{ inputs.push_to_registry }}
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          test -n "$digest"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        if: ${{ inputs.push_to_registry }}
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ matrix.arch }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  # 合并多架构镜像并推送到 registry
  merge:
    runs-on: ubuntu-24.04
    if: ${{ inputs.push_to_registry }}
    needs: build

    steps:
      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: /tmp/digests
          pattern: digests-*
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create \
            -t ${{ env.IMAGE_NAME }}:latest \
            -t ${{ env.IMAGE_NAME }}:${{ github.sha }} \
            $(printf '${{ env.IMAGE_NAME }}@sha256:%s ' *)

      - name: Inspect image
        run: |
          docker buildx imagetools inspect ${{ env.IMAGE_NAME }}:latest
